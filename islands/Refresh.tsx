export default function ({ creds }: { creds: [string, string] }) {
    return (
        <div>
            <button
                onClick={async () => {
                }}
            >
                Refresh
            </button>
        </div>
    );
}
